<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Capacitor's bridge expects a provider; the board itself never leaves
         the app's own storage. -->
    <cache-path name="cache" path="." />
    <files-path name="files" path="." />
</paths>
